# Migration notes

Append-only log. Anything noticed but deliberately not changed goes here, plus anything
that turned out differently from the plan.

---

## Owner decisions — all closed

Nothing is left open. Every finding from the design pass has been ruled on.

### Fixed

- **Scene assets are now re-hosted.** `pollSceneImage`, `pollSceneVideo` and
  `pollThumbnailBlend` previously wrote raw KIE URLs (`file.aiquickdraw.com`) into entity
  rows; those expire, so the rows eventually pointed at nothing. All three now
  `ingestUrl(..., { tier: 'durable' })` before the write. PHASE-7 §5.

- **Gender comes from the script.** `lib/gender.ts` derives it from the director's
  description of that character. When the description says nothing — or says both — no
  substitution happens. Neither original could produce that outcome; both invented a
  gender. PHASE-6 §3.3.

- **ffmpeg** runs in a Cloudflare Container (`workers/ffmpeg/`). Original flags copied
  verbatim. The in-browser path is untouched and stays the interactive default.

- **Media on Bunny with tiered retention.** `ephemeral/` swept at ~48h, `durable/` never
  on a timer, R2 cold storage separate.

### Tier rule — read this before adding any storage call

**If a URL is written into an entity row, the asset is `durable`.**

An earlier draft tiered scene images, thumbnails, voiceovers, music and SFX as
`ephemeral`. That was wrong: all of them are referenced by entity columns, and a project
may sit open for weeks. A 48h sweep would have emptied live projects — strictly worse
than the expiring provider URLs the re-hosting fixes. Only the export-time proxy cache is
genuinely `ephemeral`.

The 48h timer therefore applies to a small surface. The real cost lever for project media
is **`ARCHIVE_SWEEP_ENABLED`**: on the daily schedule, media belonging to projects marked
`archived` is deleted and the columns blanked. **Default off** — it removes generated
work, so it is never inferred. `Projects.archived` already exists and the Dashboard
already sets it.

### Model substitutions — decided, quality over cost

- `gemini-2.0-flash` → `gemini-2.5-flash`, **not** `gemini-3.1-flash-lite`. 39 call sites
  including the whole prompt engine. flash-lite is ~40% cheaper on output (the dominant
  cost), but a "lite" model doing nuanced visual direction is where a regression would be
  expensive and hard to attribute. Worth A/B-ing on real scene prompts once usage data
  exists — one line in `lib/models.ts`.
- `gpt-4o` → `gpt-5.4`, **not** `gpt-5-mini`. Only two live functions use it, so the 10×
  saving is noise and there is nothing to buy by risking Flow/Re-make output.

### Still open — but not blocking

- **`ImageProviderSelector.jsx:8` copy is now false.** It reads "Base44 image generation
  first, then your configured API fallback." Auto now runs the KIE cascade. User-visible
  text; owner to reword. PHASE-12 §5.
- **Whisper word-timing quality is unmeasured.** If it disappoints, adding an AssemblyAI
  key restores the original engine with no code change.

## Noted during design, deliberately unchanged

- **`enhancePrompt`** is byte-identical to `rephraseScenePrompt` and only an orphan calls
  it. Not ported.
- **`extract_best_moments`** in `quickPublishTranscribe` is dead — no caller. Dropped.
- **The OpenShorts manifest keeps its lost-update race** (concurrent saves, last writer
  wins). Preserved from the Bunny original. The fix would be a real table, not a lock.
- **`voiceover_chunks` / `voiceover_total_chunks`** on `ProductionSettings` are almost
  certainly vestigial — AI33 accepts 1,000,000 characters per TTS call. Still written on
  every path; left alone.

## Codex log

- 2026-09-10 — Phase 10 ported all six handlers, resolved all eight generated follow-ups, and preserved the 16 non-download strings byte-for-byte after restoring original CRLF formatting. `downloadYouTubeVideo` intentionally drops three retired storage log/error strings when replacing Bunny/Base44 upload blocks with durable R2 ingestion; it contains no AI prompts. Fixed missing `ctx` propagation through `analyzeViralMoments.callAI` and removed obsolete Base44 helper arguments. Optional AI checks use the central provider-availability adapter. Contrary to §10b's description, the canonical `scheduleClipPost` really contained a `youtubeAuth.uploadVideo` branch; Phase 10 explicitly forbids publishing, so `process` now marks due rows `ready_to_post` without a network call. 38/38 Worker tests and typecheck pass, including nested inference, durable video/audio aliases, missing Cobalt configuration and non-publishing scheduling. Combined Phase 9–11 live acceptance remains open.

- 2026-09-10 — Phase 9 ported and registered all three thumbnail/LLM handlers; the two supplied LLM wrappers were already registered and remain byte-identical. Fixed the codemod's missing `ctx` argument in the nested thumbnail fallback helper and routed optional provider availability through `hasAiProvider`, preserving Claude-to-Gemini fallback with the shared inference key. All 19 prompt strings survive byte-for-byte. Temporary JavaScript-port type suppression follows earlier phases; typed cleanup remains separate. Worker typecheck, 34/34 tests, web lint/build and production health/CORS pass. Deployed Worker `af889c8d-1774-4ab6-b585-780e8d04e109`. The real thumbnail form loads; its render gate still requires a reference photo upload. Phase 9 expressly permits registering and moving on; combined Phase 9–11 UI acceptance remains open.

_Append below as you work. One line per thing you noticed and did not change, and one
per deviation from a phase doc._

- 2026-09-06 — Phase 0 blocked before repository changes: the selected repo root contains only the migration bundle; the Base44 export (`src/`, root `package.json`, and `base44/functions/`) is not present, so restructuring, deduplication, build, lint, and live-app verification cannot be performed.
- 2026-09-07 — `analyzeViralMoments`: kept manifest canonical `base44/functions/analyzeViralMoments/entry.ts`; rule 2 (provider/model superset) and rule 3 (newer model path) decided it.
- 2026-09-07 — `generateSceneBreakdown`: kept manifest canonical `base44/functions/generateSceneBreakdown/entry.ts`; rule 0 decided it because `src/lib/directApi.js` sends `__claude_passthrough` and consumes its `{ text }` response, which only this version implements.
- 2026-09-07 — `fixScenePrompts`: kept manifest canonical `base44/functions/fixScenePrompts.ts`; rule 1 confirmed the same-contract deepest copy, while `base44/functions/fixScenePrompts/entry/entry.ts` is a misfiled `explainerSceneBreakdown` handler with a different contract and was deleted as a duplicate-path misfile.
- 2026-09-07 — `pollSceneImage`: kept manifest canonical `base44/functions/pollSceneImage.ts`; rule 0 decided it because `src/pages/ContentGeneration.jsx` reads `pollData.errors`, which only the root file returns. The owner's parenthetical `entry.ts` label was inconsistent with both the manifest and verified file contents, so the call-site evidence controlled.
- 2026-09-07 — `generateScriptBatches`: kept manifest canonical `base44/functions/generateScriptBatches.ts`; rule 3 decided it because its descriptive `v6 — Sleep story FULL REWRITE` header supersedes both `v4` alternatives and explains the intentional two-way prompt replacement.
- 2026-09-07 — `shortsSceneBreakdown`: kept manifest canonical `base44/functions/shortsSceneBreakdown/entry.ts`; rule 2 (31-versus-5 prompt expansion) plus the identical deepest copy decided it. The extra `sentence_count` response field is not consumed by the frontend.
- 2026-09-07 — `explainerSceneBreakdown`: **manifest error** — kept `base44/functions/explainerSceneBreakdown/entry.ts` and rejected manifest-selected `base44/functions/explainerSceneBreakdown.js`; rule 0 decided it because `src/pages/ContentGeneration.jsx` sends `start_section` and loops on `next_section`, supported only by the section-batched `entry.ts` version. Do not re-apply the manifest selection for this function.
- 2026-09-07 — `callClaudeProxy`: kept manifest canonical `base44/functions/callClaudeProxy/entry.ts`; rule 0/2 decided it because it preserves the live prompt contract and adds the asset-proxy action without removing that path.
- 2026-09-07 — `detectFaceRegion`: kept manifest canonical `base44/functions/detectFaceRegion/entry/entry.ts`; rule 4 decided it after confirming the retained frontend sends only `image_base64` and consumes fields shared by both live-compatible versions.
- 2026-09-07 — `extractCharacterDNA`: kept manifest canonical `base44/functions/extractCharacterDNA/entry.ts`; rule 3 decided it because its descriptive v3 Gemini-primary/Claude-backup header supersedes v2.
- 2026-09-07 — `generateFullScript`: kept manifest canonical `base44/functions/generateFullScript/entry/entry.ts`; rule 2 decided it because it preserves merge mode and adds `init_explainer`.
- 2026-09-07 — `generateProgressionImage`: kept manifest canonical `base44/functions/generateProgressionImage/entry/entry.ts`; rule 2 decided it because it retains the reference-chain path and adds the later handling present only in the larger version.
- 2026-09-07 — `generateProgressionVideo`: kept manifest canonical `base44/functions/generateProgressionVideo.ts`; rule 4 decided it after the retained call-site contract matched and neither version had a decisive descriptive v3+ header.
- 2026-09-07 — `generateSceneImage`: kept manifest canonical `base44/functions/generateSceneImage.ts`; rule 0 decided it because retained callers send `preferred_provider`, which the z-image-only duplicate does not handle.
- 2026-09-07 — `generateScenePrompts`: kept manifest canonical `base44/functions/generateScenePrompts.ts`; rule 4 decided it after confirming matching call-site contracts and no decisive descriptive version header.
- 2026-09-07 — `generateSeoDescriptions`: kept manifest canonical `base44/functions/generateSeoDescriptions/entry.ts`; rule 3 decided it because its Gemini-to-Claude migration is the newer provider path.
- 2026-09-07 — `generateSeoTitlesDescriptions`: kept manifest canonical `base44/functions/generateSeoTitlesDescriptions.ts`; rule 4 decided it after matching contracts and no descriptive v3+ version marker.
- 2026-09-07 — `generateThumbnailImage`: kept manifest canonical `base44/functions/generateThumbnailImage.ts`; rule 2 decided it because it contains the AI33 SeedDream primary plus Ideogram fallback paths. The only `char_photos` callers belong to the Phase 0-dropped Quick Publish feature.
- 2026-09-07 — `generateVoiceover`: kept manifest canonical `base44/functions/generateVoiceover/entry/entry.ts`; rule 0/3 decided it because the retained voiceover panel sends both `voice_category` and `provider`, and this is the descriptive v3 three-path implementation.
- 2026-09-07 — `initializeScriptBatches`: kept manifest canonical `base44/functions/initializeScriptBatches.ts`; rule 4 decided it after confirming the retained `project_id` call contract and its matching sleep-story rewrite pipeline.
- 2026-09-07 — `listVoicesByProvider`: kept manifest canonical `base44/functions/listVoicesByProvider/entry/entry.ts`; rule 4 decided it because all retained callers use the shared `source` input and read only `voices`.
- 2026-09-07 — `longViralGenerateScript`: kept manifest canonical `base44/functions/longViralGenerateScript/entry/entry.ts`; rule 4 decided it after matching the retained call contract and finding no decisive descriptive version header.
- 2026-09-07 — `longViralSceneBreakdown`: kept manifest canonical `base44/functions/longViralSceneBreakdown/entry.ts`; rule 0 decided it because `ContentGeneration.jsx` sends `start_batch` and loops on partial responses.
- 2026-09-07 — `pollVoiceover`: kept manifest canonical `base44/functions/pollVoiceover.ts`; rule 4 decided it because the retained caller's `status`/`voiceover_url` contract is preserved and the root keeps the broader response handling.
- 2026-09-07 — `proxyFetchAsset`: kept manifest canonical `base44/functions/proxyFetchAsset/entry/entry.ts`; rule 0 decided it because retained callers consume inline `data`, which the older duplicate does not return.
- 2026-09-07 — `quickPublishTranscribe`: kept manifest canonical `base44/functions/quickPublishTranscribe/entry.ts`; rule 0/2 decided it because retained `OpenShorts` and `directApi` callers use its submit, poll, clip, and Bunny actions.
- 2026-09-07 — `selectHook`: kept manifest canonical `base44/functions/selectHook/entry/entry.ts`; rule 0 decided it because `timeline/useVideoExport.jsx` sends `action: 'proxyAsset'` and reads inline `data`.
- 2026-09-07 — `shortsGenerateScript`: **manifest error** — kept `base44/functions/shortsGenerateScript/entry/entry.ts` and rejected manifest-selected root `.ts`; rule 3 decided it because the duplicate has the descriptive v3 migration header and identifies itself as engine v5, while the root is engine v4 with the non-semantic `v2 — redeployed` stamp.
- 2026-09-07 — `sleepSceneBreakdown`: kept manifest canonical `base44/functions/sleepSceneBreakdown/entry/entry.ts`; rule 3 decided it because its descriptive v5 Gemini-primary/Claude-fallback header supersedes v3.
- 2026-09-07 — `submitTranscription`: **manifest error** — kept `base44/functions/submitTranscription/entry.ts` and rejected manifest-selected root `.ts`; rule 0 decided it because `src/lib/transcribeASR.js` reads `transcript_id` and then polls, and only `entry.ts` implements that live contract. The descriptive v5 header confirms the selection.
- 2026-09-07 — Phase 0 acceptance-gate cleanup: removed remaining dead-feature launch controls from `NewProject` and `ChannelsHub`, removed stale dashboard routing special-cases, and reworded one retained-code comment so the required dropped-feature grep is clean. `QuickShortcuts` also lost its dead Quick Publish tile because its page is deleted and the acceptance gate explicitly forbids that identifier.
- 2026-09-07 — Updated the generated dedup manifest and the Phase 6/8 batch source paths for the three verified manifest exceptions (`explainerSceneBreakdown`, `shortsGenerateScript`, `submitTranscription`) so later tooling cannot re-apply the rejected selections.
- 2026-09-07 — Removed retained-component imports and controls for deleted `AutoEditButton` and `ClipAutoPublish`; the first Phase 0 build caught these two stale references after their feature files were pruned.
- 2026-09-07 — Ran the repository ESLint fixer to remove 114 unused imports exposed by the Phase 0 lint gate. This was import-only mechanical cleanup; existing unused-variable warnings remain non-blocking under the prescribed `eslint . --quiet` command.
- 2026-09-08 — Phase 1 media configuration uses the provisioned R2 buckets and `https://media.radiantmemory.ca`; the unused Bunny variables were left empty so no placeholder or Bunny credential is mistaken for a live dependency.
- 2026-09-08 — Phase 1 Neon documentation drift: the current Neon dashboard provisions Better Auth and exposes a direct issuer/JWKS URL, not the document's legacy Stack Auth project URLs. The Worker is configured against the live Neon Auth issuer/JWKS, Neon remains AUTH ONLY, and Phase 3 must reconcile the provided `@stackframe/react` frontend adapter before auth integration proceeds.
- 2026-09-08 — Wrangler v4 warns that the provided container instance type `standard` is now named `standard-1`; retained the supplied configuration during Phase 1 unless deployment proves the legacy alias is rejected.
- 2026-09-08 — Phase 1 host limitation: Docker CLI/daemon is unavailable, so Wrangler cannot build the supplied ffmpeg image. The API is deployed with `--containers-rollout=none` for the Phase 1 health/CORS gate; the container binding/config remains intact and its image rollout must be completed before ffmpeg-dependent execution is accepted.
- 2026-09-08 — Phase 1 tooling correction: the supplied `workers/api/package.json` pinned Wrangler 3 even though the supplied `[[containers]]` syntax requires Wrangler 4. Updated the workspace dev dependency to Wrangler 4.129.1; the documented `npm run db:init:local` and `npm run db:init` commands now execute successfully.
- 2026-09-08 — The Phase 1 Vercel deployment loads, but its retained Base44-era runtime currently logs repeated `filter/map is not a function` errors. This is not a CORS failure and was not altered during provisioning; investigate while replacing the Base44 data/auth adapter in Phases 3–4.
- 2026-09-08 — Phase 1 gate passed: `myytengine-api.tolu-adebisi.workers.dev/api/health` returned HTTP 200 with `{data:{ok:true,...}}`; GET and OPTIONS probes carrying Origin `https://myytengine.vercel.app` returned that exact `Access-Control-Allow-Origin`. Remote D1 contains the expected 27 tables; Neon Postgres remains untouched.
- 2026-09-08 — Phase 2 placed the supplied frontend compatibility client files unchanged (apart from line-ending normalization): the existing `@/api/base44Client` path now re-exports the Cloudflare API client, so retained call sites were not edited.
- 2026-09-08 — Phase 2 dependency rationale: upgraded the existing `@cloudflare/vitest-pool-workers` dev dependency from 0.5 to 0.22 and Vitest from 2 to 4.1. The supplied versions predate current remote-binding support; the mandated pool package now runs the same suite against local bindings and the provisioned remote D1/R2 bindings without adding a test-only API route.
- 2026-09-08 — Phase 2 test configs use compatibility date `2026-08-22`, the newest date supported by `@cloudflare/vitest-pool-workers@0.22.0`'s bundled workerd. This is test-only; the production Worker compatibility date and behavior were not changed.
- 2026-09-08 — Phase 2 acceptance passed: all nine D1/R2 parity cases passed locally and against remote bindings, including partial merges, attrs overflow, promoted `UploadMetadata.record_type`, type preservation, ordering, and the >64 KB cold-field round-trip plus purge. Remote cleanup left the four exercised tables empty.
- 2026-09-08 — `npm install` reports 29 transitive dependency advisories (2 low, 8 moderate, 18 high, 1 critical). No automatic audit fix was applied because that could introduce behavior-changing upgrades outside this migration phase.
- 2026-09-08 — Phase 3 documentation correction: Neon replaced its legacy Stack Auth integration with Managed Better Auth. The supplied `@stackframe/react` adapter and `VITE_STACK_*` variables were not installed; the SPA now uses the current `@neondatabase/neon-js` React adapter plus `@neondatabase/auth-ui` and the branch's single `VITE_NEON_AUTH_URL`, as prescribed by Neon's current React and legacy-auth migration guides.
- 2026-09-08 — Phase 3 dependency rationale: added `@neondatabase/neon-js@0.7.0-beta` for the branch-aware Managed Better Auth session/JWT client and `@neondatabase/auth-ui@0.3.0-beta` for the sign-in/sign-up handler views. These are Neon's current documented packages; `@stackframe/react` would target the retired provider contract provisioned by neither the live project nor the current Neon console.
- 2026-09-08 — Phase 3 Worker auth correction: the live JWKS reports `kty=OKP`, `alg=EdDSA`, `crv=Ed25519`; added Ed25519 verification and corrected issuer/audience validation to the Neon Auth host origin. A generated Ed25519 JWT test now exercises this exact contract, and all 10 Worker tests pass.
- 2026-09-08 — Phase 3 partial gate: the deployed signed-out SPA redirects to `/handler/sign-in` with no browser warnings, and unauthenticated production requests to both `/api/db/*` and `/api/fn/*` return HTTP 401 with the Vercel CORS origin. The signed-in Dashboard and sign-out checks remain pending a trusted-domain update and a test user/session.
- 2026-09-08 — The broad `apps/web` JavaScript typecheck remains non-gating because it reports the existing generated JSX prop-inference backlog across many retained pages plus Phase 2 client JS inference. Phase 3's production bundle, full ESLint gate, Worker typecheck, and focused auth/parity tests pass; no unrelated page typings were changed.
- 2026-09-08 — Phase 3 same-origin auth correction: a direct Vercel external rewrite to Neon Auth returned `INVALID_HOSTNAME` because the original host header was preserved. Added a streamed `/api/neon-auth/*` Worker relay, rewrote the SPA's `/api/auth/*` path through it, removed upstream cookie `Domain` attributes, and retained all other response headers (including Neon's JWT header). This also avoids relying on third-party cookies for the production session.
- 2026-09-08 — Phase 3 types-only dependency correction: upgraded `@cloudflare/workers-types` to `5.20260908.1` so the relay's standards-based `Headers.getSetCookie()` use is represented by the current Worker type definitions. Runtime dependencies were not changed by this upgrade.
- 2026-09-08 — Phase 3 trusted-domain and test-user gate: added `https://myytengine.vercel.app` to Neon Auth trusted domains. Created disposable users `phase3-gate-20260908132820@myytengine.invalid` and `phase3-gate-20260908190039@myytengine.invalid`; the second replaces the first only for testing because the first one's intentionally unpersisted password was lost when the browser-control session reset. Neither user was deleted because deletion was not authorized.
- 2026-09-08 — Phase 3 SDK correction: the Managed Better Auth React adapter does not expose a `token()` client action. It injects the short-lived JWT from the `set-auth-jwt` response header into `getSession().data.session.token`; the API token provider now resolves that supported value at request time, avoiding a first-render null-session race.
- 2026-09-08 — Phase 3 acceptance passed in production: signed-out `/` redirects to `/handler/sign-in`; the disposable user signs in and reaches the Dashboard with D1-backed zero-count cards and no browser warnings/errors; the browser sends a JWT that passes Worker Ed25519 issuer/audience verification; `/handler/sign-out` clears the session; and revisiting `/` redirects to sign-in. Local gates are Worker typecheck, 11/11 Worker tests, web ESLint, web production build, and `git diff --check`. Production versions: Worker `0b720820-5284-4b4f-9350-886982216211`, SPA `dpl_E3XG6HsNTPW5G4SY2k6xHk8pdW7G`.
- 2026-09-08 — Phase 4 supplied-client correction: `apps/web/src/api/client.js` still contained the retired `KNOWN_FLAT`/`resolvedShape` compatibility branch and stripped `/entry` suffixes, contradicting Phase 4's explicit zero-match gate. Removed that normalization so function names are sent directly to `/api/fn/:name`.
- 2026-09-08 — Phase 4 supplied-key-route correction: `POST /api/keys/set` returned only `{ provider, configured }`, while the phase requires the write response itself to return a masked hint. Added `hintOf(value)` to the response and regression coverage proving neither the write nor list response exposes plaintext, ciphertext, or IV.
- 2026-09-08 — Phase 4 CORS hardening: an adversarial live probe found that an unapproved Origin received the first configured origin instead of receiving no allow-origin header. Browsers still rejected the mismatch, but this did not express the exact-origin policy correctly. `corsHeaders` now emits `Access-Control-Allow-Origin` only for an exact configured match; regression tests cover allowed, disallowed, absent, and wildcard-free behavior.
- 2026-09-08 — Phase 4 live test fixture: the disposable Phase 3 account stores the non-secret Worker URL under `COBALT_API_URL` solely to exercise encrypted persistence, masked reload hints, Test All, and the configured-provider health row without transmitting a real provider credential. It is not a production Cobalt endpoint and must be replaced or removed before Clip Extractor execution.
- 2026-09-08 — Phase 4 acceptance passed in production: Settings renders the complete grouped catalog and tier badges; deliberately invalid input fails its probe; the owner configured CheaperInference, KIE.ai, and AI33.pro and each live probe succeeds; Test All reports all four configured entries successful; a full reload leaves all 13 entry fields empty and shows masked hints only; and remote D1 reports four non-empty ciphertext/IV rows with four masked hints. Dashboard health reports `9 OK` with D1, both R2 buckets, model mapping, ffmpeg binding, and every configured provider. No plaintext provider credential was read into logs, shell output, or repository files. Production versions remain Worker `b3032956-f0b0-4858-98e4-33d7ca43f54e` and SPA `dpl_BSXrb8xC3WJVhfQcp1aoP7te8Cbt`.
- 2026-09-09 — Phase 5 bundle correction: `MIGRATION-PLAN.md` requires `lib/youtube.ts` and `lib/stock.ts`, but neither file existed in the supplied migration tree. Added thin raw-response adapters for the retained read-only YouTube Data API and Pexels/Pixabay request paths; upload/publish behavior remains absent as required by Phase 0.
- 2026-09-09 — Phase 5 adapter smoke found and fixed two contained normalization defects: the OpenAI convenience wrapper now applies the same aggregator-only model remapping as `openaiFetch`, and KIE `recordInfo` now includes `.video_url` in `urls` instead of allowing an empty-array fallback to short-circuit it.
- 2026-09-09 — Phase 5 ASR spike: direct binary REST inference with `@cf/openai/whisper-large-v3-turbo` returned 90 genuine `{word,start,end}` objects, but its Workers AI binding rejected the documented string/array/structured-binary forms in both remote tests and a temporary deployed Worker. The prescribed fallback `@cf/deepgram/nova-3` accepted the same audio stream through the binding and returned 87 genuine word timestamps. `lib/asr.ts` now uses Nova-3 internally while preserving the existing `submit`/`poll` interface and `workers-ai` setting value. The isolated `myytengine-phase5-smoke` Worker was deleted after the measurement.
- 2026-09-09 — Phase 5 acceptance passed: all eight named adapter modules are present; the deterministic adapter suite hits AI, ASR, R2, KIE, AI33, YouTube, stock, and HTTP response shapes (26/26 total Worker tests), Worker typecheck passes, the live remote ASR smoke passes with 87 word timestamps, and Phase 4's live provider probes remain green for the configured CheaperInference, KIE, and AI33 credentials.
- 2026-09-09 — Deployed the Phase 5 adapter checkpoint as Worker version `1f90221b-1540-42ec-a877-8f2a7a2c01f0` with container rollout still disabled. Production `/api/health` returned HTTP 200 `{data:{ok:true}}` and the exact Vercel CORS origin after deployment.
- 2026-09-09 — Phase 6 ported and registered all 18 core-text functions. Independent post-fix verification passed for every module with 1,201 strings of at least 40 characters preserved byte-for-byte; the phase document's stated 1,205 total is stale. All generated `PORT-TODO` markers are resolved. The former JavaScript modules retain temporary `// @ts-nocheck` headers as explicitly permitted Phase 6 debt; Worker typecheck, 26/26 Worker tests, web ESLint, and the production web build pass.
- 2026-09-09 — Phase 6 provider/context corrections were mechanical only: direct OpenAI SDK remnants now use the shared adapter, aggregator-mode provider availability is resolved through `hasAiProvider`, helper call sites receive `ctx`, and `generateSceneBreakdown` calls `explainerSceneBreakdown` in-process. The owner-directed gender fix is applied in both `enhanceScenePrompts` and `rephraseScenePrompt`: the shared `sanitizeGender(fullDesc, identityDesc)` is used and no gender is invented when the director supplied none.
- 2026-09-09 — Phase 6 live testing exposed and fixed two source/runtime defects without changing prompts: `generateFullScript` merge helpers still referenced a nonexistent `base44` argument, and `generateSceneBreakdown` logged `avgSceneDuration` outside the sleep-only scope where it had been declared. The deployed checkpoint containing both fixes is Worker version `e82757bf-744d-46d5-a1dc-1ea0a5770c3c`.
- 2026-09-09 — Phase 6 production E2E used project `f34e9749d15948c7b992d5dbc7ab5b63` and completed Duration → Script → Scene Breakdown → Scene Prompts in the real SPA. D1 finished at project status `content_generation` with 89/89 rows in `prompts_ready` and zero remaining `DIRECTOR_NOTES` rows; production health is HTTP 200 and both health and function preflight return the exact Vercel CORS origin. The prescribed Topics step cannot run sequentially because `generateTopics` is assigned to Phase 11, and the retained UI has removed the separate Hooks step in favor of hooks embedded during script generation, so the direct-topic path was used. `extractCharacterDNA` also returned a non-fatal 404 exactly as expected before its Phase 7 port.
- 2026-09-09 — Phase 6 observed inherited latency/idempotency debt: the configured Gemini route rejected several scene requests and Claude fallback calls sometimes returned HTTP 524; one `generateSceneBreakdown` invocation took 526 seconds, beyond the unchanged 300-second client timeout. The frontend retry plus a still-running Worker invocation produced 89 rows but only 60 distinct `scene_number` values. No CPU-limit event occurred and no batch/model/prompt/client-timeout behavior was changed under the prime directive. A live legacy Base44 URL/credential was not supplied, so the requested same-seed Base44-vs-Worker output comparison could not be executed; prompt multiset equality, call contracts, status transitions, and the live Worker structure were verified instead.
- 2026-09-09 — Phase 7 ported and registered all 14 batch functions; the two hand-written storage functions were already present and byte-identical to the supplied migration versions. The initial batch gate passed 14/14 with 244 strings (the phase document says 246) and 13 generated TODO markers (the phase document says 3). After owner-directed visual-provider removal, the generic string verifier necessarily reports only deleted/renamed provider log and error strings in the six affected files; all actual generation prompt templates remain intact, and the eight untouched ports still pass independently.
- 2026-09-09 — Phase 7 consolidated all image/edit/video work on KIE, removed retired visual-provider and S3 credential paths, replaced thumbnail storage with the Worker R2 binding, and re-hosts scene images, scene videos, thumbnail polls, and thumbnail blends into durable storage before entity writes. `PHASE-7.md` contradicts itself by directing this Step 5 re-hosting and requiring it in acceptance, then saying not to start Step 5; the owner-directed fix and acceptance criteria control.
- 2026-09-09 — Phase 7 production media gate used Scene `6f023ff729d44b029c00472a6d3501bc` in project `f34e9749d15948c7b992d5dbc7ab5b63`: the real SPA submitted and polled the image to `image_generated`, rendered it, animated it to `video_ready`, and D1 stores both URLs under `https://media.radiantmemory.ca/durable/...`. Worker typecheck, 26/26 Worker tests, web build/lint, static S3/credential/provider greps, and all 16 registry entries pass. Deployed Worker version: `f8ef1429-d0d1-4f0a-9338-ec993641116a`.
- 2026-09-09 — Phase 7 dependency-deferred UI checks: the retained thumbnail UI cannot create its prerequisite concept until `newThumbnailConcept` is ported in Phase 9, so the thumbnail poll gate will be completed after Phase 9. Browser exports at 480p and 1080p completed and played, but were 0.4 MB and 1.5 MB because only one of 89 scenes currently has media; a synthetic 14-minute unsaved timeline stalled while loading the 88 empty scene rows and was cancelled, so the >10 MB multipart UI check remains deferred until a complete-media fixture exists. Unsaved duration changes were discarded by reloading and no project timeline data was saved.
- 2026-09-09 — Phase 8 batch correction: Phase 0 intentionally deleted every `base44/functions/inworldVoiceover*` source while Phase 8 still requires the function and its retained `VoiceoverPanel` call contract. Recovered the exact canonical source from the pre-Phase-0 Git commit into `migration/tools/sources/inworldVoiceover.ts` and pointed only the migration batch at that fixture; the deleted Base44 runtime copy stays deleted. The corrected batch passes 11/11 with all 37 original strings intact and the documented 18 manual follow-ups.
- 2026-09-09 — Phase 8 audio storage/provider work: synchronous MiniMax and chunked Inworld output use the `MEDIA` R2 binding with their existing `voiceover/...` keys, while async MiniMax/AI33 voiceovers are ingested into durable storage before `ProductionSettings` or `Projects` receives the URL. Music submission/polling now uses AI33 Suno and durable storage. `MusicPanel.jsx` only stops polling on `COMPLETED`/`completed` or failure, so `checkMusicStatus` preserves the live `COMPLETED` response and `completed` entity status rather than the phase snippet's incompatible `SUCCESS`/`ready` vocabulary.
- 2026-09-09 — Phase 8 prompt audit: eight untouched/provider-compatible ports pass the post-edit prompt verifier; `generateMusic` also passes after retaining its former negative-tag string for audit traceability. `checkMusicStatus` and the two transcription wrappers intentionally lose only retired provider endpoint URL strings because Phase 8 explicitly replaces KIE music polling and direct AssemblyAI calls with AI33 and `lib/asr.ts`; no generation prompt was altered.
- 2026-09-09 — Phase 8 document drift: the supplied post-design storage/ffmpeg implementation supersedes two literal acceptance greps. `lib/storage.ts` and `lib/ffmpeg.ts` retain a server-only optional Bunny backend, while production is configured `MEDIA_BACKEND="r2"`; retained OpenShorts actions keep their historical `bunny_save_project`/`bunny_list_projects`/`bunny_delete_project` names because the phase requires that action interface to survive. Likewise, `quickPublishTranscribe.clip_video` already calls the owner-selected Cloudflare ffmpeg Container design and degrades to the prescribed 501 while the container is unavailable, so the older Phase 8 “owner decision needed” block is already resolved rather than reopened.
- 2026-09-09 — Phase 8 owner-directed UI change: removed the MiniMax-via-AI33 and unavailable Inworld cards from Content Generation after the live MiniMax catalog path returned AI33 `voice_not_found` and the owner explicitly discarded both sections. Added a supported “Use your own voiceover” workflow that uploads audio through the authenticated `/api/upload` route into durable R2, marks `ProductionSettings.voiceover_status` completed with `selected_voice_id='uploaded'`, and mirrors the URL to the project so timeline/export consumers use it.
- 2026-09-10 — Phase 8 production audio gate: the AI33 catalog loaded 223 voices and sample preview playback worked; a short real voiceover progressed through `generating` to `completed`; “Rise of the Unseen” completed through `generateMusic`/`checkMusicStatus`; and the temporary Scene `phase8-sfx-scene-20260909` produced a real control-room effect rather than spoken prompt text. Voiceover, music and SFX all persisted under `https://media.radiantmemory.ca/durable/...` and played in the real SPA. The new owner-directed upload card also accepted an MP3 and exposed its native preview before persistence.
- 2026-09-10 — Phase 8 OpenShorts dependency contradiction: the real `yes.mp4` upload reached durable R2 and completed the speech-to-text handoff, then failed at the next step with `Unknown function: analyzeViralMoments`. That handler is explicitly assigned to Phase 10, even though the Phase 8 gate asks the same UI to continue through analysis and manifest save/list/delete while Phase 9–10 declares that Phase 8 must already be green. The R2 manifest save/list/delete contract passes its Worker round-trip test; the full real-UI continuation is deferred until the prescribed Phase 10 handler is ported rather than pulling Phase 10 code forward out of order. Keep project `phase8-gate-20260909` and its exact test rows/assets until that retry, then clean them up.
- 2026-09-10 — Phase 8 UI architecture labels: OpenShorts still advertised Bunny-only storage and AssemblyAI-only transcription even though production runs R2 and supports the AssemblyAI/Workers-AI fallback. Updated only those stale labels, progress/error copy and comments; compatibility action names such as `bunny_save_project` remain unchanged because the phase requires their contract. Production SPA checkpoint: `dpl_AAyPnFGNimw462PTYAudPtwmfUWV`; Worker checkpoint: `f2ede2d0-c56f-4094-8a10-e07e924cb98f`.

- 2026-09-11 — Phase 11 ported and registered all eight handlers (batch 8/8, 50 strings preserved byte-for-byte, zero manual items). Post-batch inspection still found migration-introduced defects and fixed them without touching prompts: aggregator-only accounts now pass optional-provider checks through `hasAiProvider` instead of requiring direct Gemini/Claude/OpenAI keys; `ctx` now propagates through the SEO tag/hashtag and provider-fallback helpers; two invalid codemod `HttpError(..., raw: ...)` statements became the standard `{ error }` envelope (raw model output is still logged by the existing statements). `src/fn/registry.ts` holds 67 handlers and 67 handler files — one more than PHASE-9-10-11's "66" because both hand-written LLM entry points count. The complete call audit was re-run cleanly: all 58 literal `invoke('…')` names resolve, the only dynamic site (`AutoBrollButton.jsx`) resolves both `autoBrollPopulate` and `sleepBrollPopulate`, and `ContentGeneration.invokeWithTimeout` is defined but has no callers. 42/42 Worker tests, typecheck, web lint/build and `git diff --check` pass.
- 2026-09-11 — `apps/web/src/lib/renderShortCloud.js` still invoked the Phase 0-dropped `renderShortCreatomate` / `pollCreatomateRender`. It now goes directly to its own pre-existing basic 9:16 ffmpeg clip fallback and keeps the same `{ url, id, fallback }` return shape. No paid renderer was reintroduced.
- 2026-09-11 — `lib/ai.ts` accepted `response_json_schema` but never transmitted it, so the live thumbnail auto-summary returned a nested object and `summary.trim` crashed the form. `invokeLLM` now appends the caller's JSON Schema to the system instruction; the caller's prompt is unchanged and `test/phase9.spec.ts` asserts the forwarding. Deployed as Worker `c1da059e-5304-43d5-9595-132f7ba7a410`; the successful live rerun of that form is still pending a browser session.
- 2026-09-11 — Sleep b-roll contract discrepancy (PHASE-9-10-11 §11b): `autoBrollPopulate` genuinely caps at 20 scenes per call and returns `{ populated, total, remaining, done }`, but the canonical `sleepBrollPopulate` processes all of its 8-scene batches in one call and returns `{ success, populated, total, results }` with no `done`/`remaining`. `AutoBrollButton` treats a missing `done` as finished (`data.done !== false`), so the sleep path completes in one loop iteration exactly as on Base44. Ported faithfully; the sleep algorithm was not rewritten to the documented resume contract.
- 2026-09-11 — ffmpeg storage mismatch resolved: `lib/ffmpeg.ts` built a Bunny PUT target for every op while production runs `MEDIA_BACKEND="r2"` with empty Bunny configuration, so cloud clipping would have failed with "Bunny storage is not configured" even after the container ships. On the R2 backend the Worker now sends no upload target; the container returns the encoded file (`video/mp4`, `X-Ffmpeg-Duration`, `X-Ffmpeg-Bytes`) and the Worker writes it through the MEDIA binding — no storage credential of any kind reaches the container. The pre-authorized PUT path remains for the optional server-only Bunny backend. ffmpeg flags are unchanged and were diffed against `base44/functions/quickPublishTranscribe/entry.ts` lines 298–310; the only addition is the pre-existing `-protocol_whitelist` security pin. `test/ffmpeg.spec.ts` covers the R2 store, the 502/501 envelopes and the Bunny guard.
- 2026-09-11 — `healthCheck` reported "ffmpeg container: ok" whenever the Durable Object binding existed, which is exactly the state left by `--containers-rollout=none`. It now calls the container's `GET /health` with a 20-second bound and reports the failure reason, so a binding without a rolled-out image reads as an error, not a running service. Each successful probe briefly starts one billed container instance.
- 2026-09-11 — Docker and Go are both absent on this Windows host, so the `main.go` change is compile-checked only by the image build. Added `.github/workflows/deploy-worker-containers.yml` (manual `workflow_dispatch` only — never on push) to typecheck, test, build and smoke the image on a Docker-equipped runner and then run a full `wrangler deploy` with container rollout. It needs `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID` repository secrets that only the owner can create; it reads no provider key or vault secret.
- 2026-09-11 — Live-state reconciliation at handoff: Worker head is `c1da059e-5304-43d5-9595-132f7ba7a410`. The production SPA is `dpl_3wE7VXd13TiLE1BRwD4KEFoJpCXN` (2026-09-10 22:36 EDT), newer than the handoff's `dpl_4QjAtuxWQX5vqWaWG3Dsjza11bBw`, and its single bundle is byte-identical to a fresh local build of the working tree. The Vercel project has no Git integration (CLI uploads only), so pushing to GitHub does not redeploy anything. Remote D1 holds 110 rows of newly created test data only (4 Projects, 90 Scenes, 3 Scripts, 3 MusicTracks, 2 ProductionSettings, 2 ScriptBatches, 1 Topic, 1 UploadMetadata) plus 4 `UserApiKeys` rows, all owned by the disposable Phase 3 user — the owner's own account has no keys configured yet. R2 `myytengine-media` holds 8 objects (19.2 MB) and `myytengine-cold` holds 1. No legacy Base44 data has been migrated. A read-only `wrangler d1 export` snapshot was written to `Documents/Codex/backups/myytengine/` (outside Git) before any Phase 12/13 work.
- 2026-09-11 — Blocked live gates: the owner's Chrome profile has no myytengine session and the agent does not enter credentials, so the combined Phase 9–11 UI acceptance, the voiceover persistence/timeline/export check, the >10 MB multipart export gate and the Phase 12 start all wait on the owner signing in to the dedicated test tab. Cobalt still has only the fake fixture URL on the test account. Base44 export inputs (`BASE44_APP_ID`, `BASE44_TOKEN`, `BASE44_BASE_URL`) exist nowhere in the repository or its history (`base44/.app.jsonc` is git-ignored and absent), so Phase 13 cannot start without the owner supplying them.
- 2026-09-11 — Correction to the live-state entry above: the Vercel project IS Git-integrated. Pushing `fbabeb12`/`1b047298` to GitHub produced production deployment `dpl_DKgFzqzAKDXrGbLP6EkaUJwboc9a` within a minute (aliased to https://myytengine.vercel.app). Its bundle is byte-identical to the local build of the same tree, so nothing regressed, but every future push to `main` redeploys the SPA — commit only trees whose `apps/web` build has been verified. The Worker is not Git-deployed; it remains a manual `wrangler deploy` (now version `2c6e3cf4-ef1a-49c3-9cd0-c663939df1e5`, container rollout still disabled).
- 2026-09-11 — Phase 13 Step 0 remote parity: `vitest.remote.config.ts` binds the PRODUCTION D1 (`myytengine`) and cold bucket, and `test/db.spec.ts` test 1 asserts four tables are empty, which is no longer true. The scoped remote run was not executed in this session (blocked by the agent's permission policy for actions against production data); the owner should run `npx vitest run --config vitest.remote.config.ts test/db.spec.ts` from `workers/api` and expect only test 1 to fail for that reason — tests 2–9 create and delete their own uniquely-keyed rows.
- 2026-09-11 — Owner direction: "we are doing away totally with base44". Phase 12 executed on that authority while the Phase 9–11 live UI checks remain open (they need a signed-in browser session and are unaffected by this phase, which changes no backend behavior). `debase44.mjs --write` rewrote 66 imports across 208 files (the tool's own header says 104; Phase 0 pruning explains the difference). The codemod missed one dynamic `import('@/api/base44Client')` in `components/channels/TopicStatusPanel.jsx`; it now imports `@/api/client` directly — without that fix, deleting the shim would have broken topic status lookups at runtime. Deleted `src/api/base44Client.js` and the stranded Deno `src/functions/callClaudeProxy.js` (`src/lib/app-params.js` was already gone). Removed `@base44/sdk` and `@base44/vite-plugin` (lockfile now has zero `@base44` entries), replaced the plugin with the explicit `@` alias while keeping the `/api/auth` dev proxy, renamed the package `base44-app` → `@myytengine/web`, and committed `apps/web/.env.example` (`VITE_API_BASE`, `VITE_NEON_AUTH_URL`; the doc's `VITE_STACK_*` names are obsolete) with a `.gitignore` exception. Vercel has no `VITE_BASE44_*` variables to delete; `vercel.json` already carried the SPA fallback plus the auth relay rewrite and is unchanged. The `base44/` reference tree (137 files) is deleted per START-HERE §3; the canonical sources remain in Git history at `e84353ec` and earlier for any future prompt verification. The local `base44` identifier is intentionally kept at its ~440 use sites per PHASE-12 §2. `ImageProviderSelector.jsx` user-visible copy now reads "Best available model, with automatic fallback." — the phase document's own minimal wording, applied under the owner's blanket authorization; owner may still reword. Remaining `Base44` mentions in `client.js`, `AuthContext.jsx`, `invokeLLM.js` and `ClipExtractor.jsx` were comments only and were reworded so `grep -ri base44 apps/web/src` returns only the kept identifier. Worker keeps `proxyFetchAsset`'s `base44.app` host allowlist entries and `analyzeViralMoments`'s `model_used: 'base44_llm'` response value because both are runtime contracts, not dependencies.
- 2026-09-11 — Phase 12 deployed and verified live: Git-triggered production deployment `dpl_2U5m1fRqHqxmoDtDBpd2REkwXmF3` serves the rebuilt bundle (`index-CC2ibKC3.js`, identical to the local build), a hard refresh of `/TimelineEditor` returns the app shell with HTTP 200, the `/api/auth/*` relay answers through the Vercel rewrite, the signed-in session survived the deploy, and the new "Auto (Best Available)" copy is visible in Content Generation. Dashboard HealthCheck reports Model mapping, D1, R2 media and R2 cold OK and — after the probe fix — "ffmpeg container" as an error after its bounded 20 s wait instead of a false OK. Owner-account key round-trip could not be exercised because that account has no keys (next item).
- 2026-09-11 — Account/key finding that gates every generation check: the live browser session is the owner's real account (`tolu.adebisi@gmail.com`, user `ea2ab4a8-0f37-482e-8760-b84966346ec3`), which holds NO BYOK rows; all four configured keys (CheaperInference, KIE, AI33, the fake Cobalt fixture) belong to the disposable Phase 3 user `e461bb79-…`. Entity reads are not user-scoped, so the owner sees the four test projects, but `ctx.keys` is per-user, so HealthCheck shows CheaperInference/KIE/AI33 as missing and the AI33 voice catalog shows "0 voices" for the owner. The agent does not enter API keys; the owner must add them under Settings for this account (or sign in as the test user) before thumbnails, topics, niche strategy, SEO, OpenShorts analysis and b-roll can be exercised. Pexels/Pixabay keys are also required for any b-roll run and are configured for neither account.
- 2026-09-11 — >10 MB multipart gate (API-level): from the signed-in SPA origin, the exact `uploadToR2` init → 3×chunk (5 MiB, 5 MiB, 2 MiB) → complete sequence that `ExportContext.uploadToR2` performs stored a 12,582,912-byte random `video/mp4` object at `exports/tolu.adebisi@gmail.com/phase8-gate-20260909/1789099480952_multipart-gate-12mb.mp4` in 3.4 s; a HEAD on the public URL returned 200 with the full Content-Length, and chunk sizes of 2–5 MiB all succeed individually. The object was deleted afterwards (the CDN edge may serve a cached copy briefly). This proves the R2 multipart contract end to end but is not a full-media browser export; a complete-media fixture is still needed for that.
- 2026-09-11 — Owner-requested voiceover upload verified through the real "Use your own voiceover" card on the disposable project `phase8-gate-20260909` with a generated 4-second 440 Hz WAV fixture (176,444 bytes): the card showed "Voiceover saved. Timeline and export will use this audio." with a Ready badge; D1 `ProductionSettings.cd9e06e786ba40c6849fc6de23a5a62c` now has `voiceover_status='completed'`, `selected_voice_id='uploaded'`, `voiceover_url=https://media.radiantmemory.ca/durable/2026-09-11/uploads/453ba05b7da246858422a98e63a669ca_test-voiceover-tone-4s.wav`, and `Projects.attrs.voiceover_url` mirrors it; after a full reload the card re-renders the upload, and `/TimelineEditor` binds its voiceover audio element to the same URL. The served bytes are identical to the fixture and the domain answers Range requests with 206. Duration measurement and playback could NOT be observed because the automation tab is `visibilityState=hidden`, where Chrome defers all media loading (even the project's pre-existing MP3 and Chrome's native player stall) — that is an environment limitation, not an application defect; the owner can confirm playback in a visible tab. Recovery values for that project's previous narration: `selected_voice_id='elevenlabs_EXAVITQu4vr4xnSDxMaL'`, `voiceover_url=https://media.radiantmemory.ca/durable/2026-09-10/voiceover/c7a31ed01a4546dca0ce671f7dcae0d3_c369cb52-d805-4838-a948-faceac095f97_1789010865644.mp3`; the test WAV was deliberately left in place for the owner's playback check.
- 2026-09-11 — `myytengine-media` has no R2 CORS policy (preflight on the custom domain returns 403). Not a defect for current flows: timeline duration uses a plain `Audio` element and export pulls every remote asset through `selectHook.proxyAsset` into blob URLs. Any future feature that `fetch()`es media directly from the browser will need a bucket CORS rule for the SPA origin.
- 2026-09-11 — Phase 13 closed as a NO-MIGRATION cutover by owner decision ("yes for phase 13... dont move any data"). No Base44 export was run and no legacy rows or media were imported; the new stack starts from the data created during Phases 2–12 (remote D1: 110 rows across 27 tables — 4 Projects, 90 Scenes, 3 Scripts, 3 MusicTracks, 2 ProductionSettings, 2 ScriptBatches, 1 Topic, 1 UploadMetadata, 4 UserApiKeys; R2 media 8 objects / 19.2 MB plus the Phase 8 voiceover upload, cold 1 object). Pre-cutover D1 snapshot: `Documents/Codex/backups/myytengine/d1-pre-phase12-20260911T033240Z.sql` (outside Git). The temporary id-preserving `import` op is removed from `workers/api/src/routes/db.ts` (`test/db-route.spec.ts` asserts it now 404s); `db/client.ts` keeps `importRows` for the parity test and any future deliberate re-enable. `migration/tools/export-base44.mjs` and `import-to-worker.mjs` stay in the repo as unused tooling. Rollback to Base44 is not a data question — the old app was never decommissioned by this migration; Phase 12 commit `381e7a99` is the single revert point for the frontend.
- 2026-09-11 — Owner authorized wiring every reachable platform. The agent's own boundaries (no dashboard sign-ins, no handling of provider secrets or access tokens) plus the session's permission policy mean three items stay with the owner: adding the three provider keys in Settings under the owner's login (or signing in as the Phase 3 test user that already holds them), adding the two repository secrets the container workflow needs, and Pexels/Pixabay keys for b-roll. WSL, Docker and Go are all absent on this host.
- 2026-09-11 — Live Phase 9 thumbnail gate found a real migration defect: `newThumbnailConcept.ts` called `geminiFetch(ctx, '/v1beta/models/${geminiModel}:generateContent', …)` with a plain single-quoted string, so the aggregator received the literal model id `${geminiModel}` and answered HTTP 400 "not listed on the marketplace". The original (`base44/functions/newThumbnailConcept.ts` line 250 at `e84353ec`) used a template literal; the port lost the backticks. Restored the template literal (no prompt or model change — it still pins `gemini-2.0-flash`, mapped in transit to `gemini-2.5-flash`), swept every other handler for the same shape (none), and `test/phase9.spec.ts` now asserts the outgoing model id.
- 2026-09-11 — Owner-account key wiring: at the owner's request the three encrypted `UserApiKeys` rows (CheaperInference, KIE, AI33) were copied server-side from the disposable Phase 3 user to `tolu.adebisi@gmail.com` (`INSERT OR IGNORE … SELECT ciphertext, iv, hint`; the vault's AES-GCM ciphertext is not bound to the user id, so no plaintext was ever read). HealthCheck for the owner now reports CheaperInference, KIE.ai and AI33.pro OK (Pexels, Pixabay, AssemblyAI, Cobalt remain unconfigured). The fake `COBALT_API_URL` fixture was deliberately not copied.
- 2026-09-11 — Live Phase 9–11 checks passed on the owner's account in the real SPA: (a) PostProduction on the Apollo project generated the full SEO package — `generateSeoTitlesDescriptions` then `generateSeoDescriptions` — and after a full reload 10 titles with the keyword panel plus three description variants hydrate from `UploadMetadata` (titles_json 4,182 chars, descriptions_json 8,949 chars, tags and pinned comment present). (b) Thumbnail: the auto-summary returned a 1,829-character string and the form stayed mounted; "Select from Generated Scenes" plus the "Beast Formula — Arrow Object Showcase" template produced 5 concepts (after the `newThumbnailConcept` model-id fix), and the selected concept rendered through KIE to a durable 7.4 MB PNG at `https://media.radiantmemory.ca/durable/2026-09-11/thumbnails/…` persisted on `ThumbnailConcepts.14fd967a…` with `is_selected=1`; the form resets to step 0 on reload by original design. (c) Channels: "Migration Gate Test Channel" (science, id `2bdd289e…`) was created and `researchNicheStrategy` saved a 4,621-character `script_strategy` within 15 s, rendered on the channel page. (d) Topics: the importer created 3 `ChannelTopics` via `parseAndScheduleTopics` with formats short/short/long and calendar dates; the manual Schedule dialog moved one to 2026-09-15 10:30 America/New_York and D1 reflects it; the New Project "Suggest Topics" flow created disposable project `a576057d…` and `generateTopics` produced 5 topics (`topics_ready`).
- 2026-09-11 — OpenShorts findings: the upload path now runs upload → R2 → Workers AI transcription → `analyzeViralMoments` end to end. The Phase 8 fixture `yes.mp4` (10 s, AAC track) is effectively silent — in-page WebAudio RMS ≈ 0.003 throughout and an extracted ADTS AAC copy also transcribes to 0 words — so Nova-3 correctly returned no words and the app's original `words array required (from ASR)` guard fired; this was a fixture problem, not an ASR container problem (the Phase 8 narration MP3 transcribes to 15 timestamped words). With that 6-second narration the analyzer legitimately returned "No viral moments found". Left the OpenShorts advanced settings at max 2 clips / 2–6 s for the retry with a longer narration generated on the disposable project.
- 2026-09-11 — Owner-directed feature removals (explicit instruction: "remove the open shorts feature, niche finding feature, auto post feature", with the follow-up clarification selecting all four niche-finding candidates and channel topic scheduling). This is a deliberate product change on top of the finished migration, not migration drift. Removed: (1) Open Shorts — `pages/OpenShorts.jsx`, its route and Dashboard shortcut, and the `bunny_save_project` / `bunny_list_projects` / `bunny_delete_project` / `bunny_config` actions of `quickPublishTranscribe` (its `submit` / `poll` / `clip_video` actions stay because Clip Extractor and the timeline clipper use them; `renderShortCloud.js` and `analyzeViralMoments` stay for the same reason). (2) Niche finding — Dashboard `ViralTrendsPanel`, the New Project "Suggest Topics / Explore a Niche" mode with its `generateTopics` handler, `researchNicheStrategy` (no longer fired on channel creation; the "Viral Script Strategy" card is gone, `Channels.script_strategy` is left in place for existing rows), and the channel "AI Generate 100 Titles" generator. `StoryTopics` remains because the project-status routers still point `created`/`topics_ready` projects at it. (3) Auto post — `ClipScheduler` and its scheduler components in Clip Extractor, the `scheduleClipPost` handler, and channel topic scheduling (`TopicScheduleDialog`, `ContentCalendar`, `DayTopicsPanel`, the Scheduled stats on Dashboard, Channels Hub and Channel Detail). `parseAndScheduleTopics` stays because the importer relies on its format assignment; any `scheduled_date` it writes is now inert data. Registry: 64 handlers / 64 files. Worker typecheck, tests, web lint and build pass after the removal.
- 2026-09-11 — OpenShorts verification was superseded by its removal. Before that: the upload path ran end to end to analysis; the Phase 8 fixture proved silent; a 1,308-character narration was submitted to AI33 on `phase8-gate-20260909` at 06:24 UTC and was still `doing` at 06:40 when the owner's removal instruction arrived. That job may still complete later and write `ProductionSettings.voiceover_url` for the disposable project; it is harmless. Temporary R2 object `ephemeral/phase8-gate/yes.aac` was deleted; `ephemeral/phase8-gate/yes.mp4` remains as before.
- 2026-09-11 — Asset-proxy allowlist gap found while checking playback: `proxyFetchAsset`, `selectHook.proxyAsset` and `callClaudeProxy.proxyAsset` still carried the Base44-era host lists and rejected the app's own media domain (`media.radiantmemory.ca`), so the timeline exporter could not proxy durable scene media and fell back to direct cross-origin loads that the bucket did not permit. Added `ownMediaHosts(env)` in `lib/storage.ts` (hostnames of `MEDIA_PUBLIC_BASE` and `BUNNY_CDN_URL`) and merged it into all three allowlists; `test/proxy-allowlist.spec.ts` covers accept/reject. Independently set a read-only R2 CORS rule on `myytengine-media` (GET/HEAD from `https://myytengine.vercel.app` and `http://localhost:5173`, Range allowed, Content-Range/Accept-Ranges exposed); the custom domain now answers preflight 204 and GET with the SPA origin. Neither change touches prompts or call sites.
- 2026-09-11 — Railway: the `redeploy` attempt returned "Your trial has expired. Please select a plan" — the `cobalt` service cannot receive a deployment until the owner selects a Railway plan; Clip Extractor's YouTube path stays blocked on that. Pexels/Pixabay keys and the Cloudflare API token for the GitHub workflow remain owner actions (the agent does not create or enter credentials). Cloudflare docs confirm Workers Builds can build the container Dockerfile and roll out instances on production-branch pushes, and the owner's Chrome is signed into the Cloudflare dashboard, so a Git-connected build is the token-free route for the ffmpeg container.
- 2026-09-11 — Playback check closed: with the new bucket CORS rule the stored voiceover WAV fetches cross-origin from the SPA origin and decodes in Chrome's WebAudio to exactly 4.000 s at 48 kHz with peak amplitude 0.30, i.e. the uploaded file is intact and playable; the only thing that could not be observed is the audio element itself, because the automation tab is hidden.
- 2026-09-11 — ffmpeg container build path chosen: Workers Builds (Cloudflare's Git-connected CI, docs: "Dockerfile builds can run in the Workers Builds environment" and production pushes "publish the image when needed and roll out container instances"). Connected `witeboy/myytengine` to the `myytengine-api` Worker in the dashboard with production branch `main`, root directory `workers/api`, build command `npm run typecheck`, deploy command `npx wrangler deploy`, preview builds disabled; Cloudflare auto-created the scoped build token itself. From this commit on, every push to `main` builds the container image and deploys the Worker WITH container rollout — the manual `--containers-rollout=none` deploys are no longer the deployment path. This commit is the first build trigger.
